/**
 * The type of the argument.
 *
 * `user` accepts a user mention.
 *
 * `number` requires a numeric value.
 *
 * `string` accepts anything.
 */
export type ArgumentType = 'user' | 'number' | 'string'
